import { core } from "@angular/compiler";

export interface Transcript {
[x: string]: any;
  code: string;
  name: string;
  grade: string;
  gpa: number;
  credits: number;
  type: string;
}

export interface Transcript {
  code: string;
  name: string;
  grade: string;
  gpa: number;
  credits: number;
  type: string;
}
